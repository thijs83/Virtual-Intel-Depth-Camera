name: CI

on:
  push:
    branches:
    - main

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: toggle workflow command echoing
        run: |
          echo "Hello"
          echo "world"