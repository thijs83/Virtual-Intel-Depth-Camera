name: CI

on:
  push:
    branches:
    - main

jobs:
  CI:
    runs-on: [ubuntu-20.04]

    steps:
      - name: Setup ros
      - uses: actions/checkout@v3

      - name: setup environment
    	  run: echo "Hello"